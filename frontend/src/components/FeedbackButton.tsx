// Stub: the public template doesn't surface a feedback button. Agent P
// keeps its own implementation in the internal repo and overlays this
// stub at template-sync time. If a partner wants a feedback button in
// their deployment, replace this stub with a real implementation — the
// import site in VoicePage.tsx already wires it in.
export function FeedbackButton() {
  return null;
}
